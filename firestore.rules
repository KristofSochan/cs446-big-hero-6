rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for testing (prototype)
    // TODO: Add proper authentication rules for production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
